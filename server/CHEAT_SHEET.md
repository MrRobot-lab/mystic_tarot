# Шпаргалка по управлению сервером INTARIUS

## 🚀 Управление сервисом

**Статус сервиса** (работает или нет):
```bash
systemctl status intarius-api
```

**Перезапуск** (после изменений в коде):
```bash
systemctl restart intarius-api
```

**Остановка**:
```bash
systemctl stop intarius-api
```

**Запуск**:
```bash
systemctl start intarius-api
```

## 📜 Логи (ошибки и доступ)

**Смотреть логи в реальном времени**:
```bash
journalctl -u intarius-api -f
```
*(Нажми Ctrl+C, чтобы выйти)*

**Посмотреть последние 100 строк логов**:
```bash
journalctl -u intarius-api -n 100
```

## 🌐 Nginx (Веб-сервер)

**Тест конфигурации** (если менял конфиги):
```bash
nginx -t
```

**Перезагрузка Nginx**:
```bash
systemctl reload nginx
```

## 📂 Файлы

* **Код бота**: `/opt/intarius-api/app.py`
* **Виртуальное окружение**: `/opt/intarius-api/venv/`
* **Конфиг сервиса**: `/etc/systemd/system/intarius-api.service`
* **Конфиг Nginx**: `/etc/nginx/sites-available/intarius-api`
